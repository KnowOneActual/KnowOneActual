name: Latest blog post workflow
on:
  schedule: # Run workflow automatically
    - cron: '0 * * * *' # Runs every hour
  workflow_dispatch: # Run workflow manually (without waiting for the cron to be called)

permissions:
  contents: write # IMPORTANT: This allows the action to update your README

jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Pull in posts
        uses: gautamkrishnar/blog-post-workflow@master
        with:
          # Your specific feed URL
          feed_list: "https://blog.beaubremer.com/feed/feed.xml"
          
          # The "Secret Handshake" found in your fetch-posts.js file
          # This matches the Skip rule you configured in Cloudflare
          user_agent: "Beau-Bremer-Website-Blog-Fetcher/1.0"
          
          # Standard headers to ensure it looks like a valid request
          accept_header: "application/xml, text/xml, application/rss+xml"
